var simples = require('simples');

var server = {};



server.init = function(params) {
//    var port = params ?
//    exports.http = simples(params.port || 80);
}

exports.simpleServer = server;

//exports.https = simples(443, {});